#include <stdio.h>
#include <16F887.h>
#pragma config FOSC = HS
#define _XTAL_FREQ 7372800

void main(void)
{
while (1) {
int z = RA0
int a = (RC0 * z);
int b = (RC1 * z);
int c = (RC2 * z);
int d = (RC3 * z);
    }
        
}
